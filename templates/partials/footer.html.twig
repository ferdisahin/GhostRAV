	 <footer class="site-footer outer">
            <div class="site-footer-content inner">
                <section class="copyright"><a href="{{site.url}}">{{site.title}}</a> &copy; {{ "now"|date("Y") }}</section>
                <nav class="site-footer-nav">
                    <a href="{{base_url}}">Latest Posts</a>
                    {% if config.theme.social_facebook %}<a href="{{ config.theme.social_facebook }}" target="_blank" rel="noopener">Facebook</a>{% endif %}
                    {% if config.theme.social_twitter %}<a href="{{ config.theme.social_twitter }}" target="_blank" rel="noopener">Twitter</a>{% endif %}
                </nav>
            </div>
        </footer>